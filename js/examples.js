document.querySelectorAll('[data-example-copy]').forEach(button => button.addEventListener('click', async () => { const node=document.getElementById(button.dataset.exampleCopy); try { await navigator.clipboard.writeText(node.textContent); document.getElementById('copy-status').textContent='Prompt copied'; } catch { const range=document.createRange(); range.selectNodeContents(node); const selection=window.getSelection(); selection.removeAllRanges(); selection.addRange(range); document.getElementById('copy-status').textContent='Text selected. Use your copy shortcut.'; } }));